% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{explore_bulk_rnaseq}
\alias{explore_bulk_rnaseq}
\title{Explore and QC bulk RNA-seq count data}
\usage{
explore_bulk_rnaseq(
  counts,
  metadata,
  gene_col = c("gene_id", "GeneID", "gene", "Gene", "ENSEMBL", "ensembl", "ensembl_id"),
  feature_col = c("gene_name", "SYMBOL", "symbol", "gene_symbol", "external_gene_name"),
  sample_col = c("sample", "Sample", "SAMPLE"),
  annotation_cols = c("condition", "treatment", "sex", "tissue"),
  color_by = NULL,
  markers = c("Adipoq", "Lep", "Ucp1", "Amh", "Amhr2", "Ar", "Akr1c3"),
  top_variable_genes = 500,
  output_dir = "DEGgo_QC",
  width = 8,
  height = 6,
  dpi = 300
)
}
\arguments{
\item{counts}{Count table or matrix. Genes should be rows and samples columns.
If \code{counts} is a data frame, one column must contain gene identifiers.}

\item{metadata}{Sample metadata data frame.}

\item{gene_col}{Candidate gene identifier columns in \code{counts}.}

\item{feature_col}{Candidate gene symbol/name columns in \code{counts}.}

\item{sample_col}{Candidate sample identifier columns in \code{metadata}.}

\item{annotation_cols}{Metadata columns shown as annotations in heatmaps.}

\item{color_by}{Metadata column used to color library size and density plots.
If \code{NULL}, DEGgo tries tissue, condition, treatment, then sex.}

\item{markers}{Optional marker genes for marker heatmap.}

\item{top_variable_genes}{Number of top variable genes for heatmap.}

\item{output_dir}{Output directory.}

\item{width}{Plot width in inches.}

\item{height}{Plot height in inches.}

\item{dpi}{Plot resolution.}
}
\value{
Invisibly returns a list containing QC table, plots, correlation
matrix, PCA plots, top variable genes, marker heatmap object, and output path.
}
\description{
Generates pre-differential-expression quality control plots and metrics for
bulk RNA-seq count data, including library size, detected genes, expression
distribution, density curves, sample correlation, hierarchical clustering,
PCA plots, top variable gene heatmap, optional marker heatmap, and automatic
sample QC flags.
}
\examples{
\dontrun{
qc <- explore_bulk_rnaseq(counts, metadata)

cleaned <- remove_flagged_samples(
  counts = counts,
  metadata = metadata,
  qc_table = qc$qc
)

counts <- cleaned$counts
metadata <- cleaned$metadata
}
}
