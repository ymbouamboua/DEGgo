% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/public_circadian_example.R
\name{prepare_circadian_tissue}
\alias{prepare_circadian_tissue}
\title{Prepare one GSE98965 tissue for circadian analysis}
\usage{
prepare_circadian_tissue(
  expression_data,
  tissue_code = "LIV",
  min_expression = 1,
  min_samples = 3,
  log_transform = TRUE
)
}
\arguments{
\item{expression_data}{A \code{data.frame} containing the processed
GSE98965 FPKM expression table, usually returned by
\code{\link[=download_gse98965]{download_gse98965()}}.}

\item{tissue_code}{Character string specifying the GSE98965 tissue
code to extract. The default is \code{"LIV"} for liver.}

\item{min_expression}{Numeric value. Minimum FPKM expression required
for a gene to be considered expressed in a sample.}

\item{min_samples}{Positive integer. Minimum number of samples in
which a gene must reach \code{min_expression}.}

\item{log_transform}{Logical. If \code{TRUE}, expression values are
transformed as \code{log2(FPKM + 1)}.}
}
\value{
A named list containing:

\describe{
\item{expr}{Numeric genes-by-samples expression matrix.}
\item{metadata}{Sample metadata with \code{sample}, \code{tissue}, \code{ZT},
and numeric \code{time} columns.}
\item{gene_annotation}{Gene identifier and symbol annotation table.}
\item{filter_summary}{Summary of expression filtering.}
\item{tissue_code}{Selected tissue code.}
\item{assay}{Expression representation returned by the function.}
}
}
\description{
Extract one tissue from the GSE98965 baboon transcriptomic atlas,
construct a numeric genes-by-samples expression matrix, filter lowly
expressed genes, generate numeric Zeitgeber Time metadata, and prepare
gene-symbol annotations.
}
\details{
Sample columns are detected using the pattern
\code{TISSUE.ZTXX}, for example \code{LIV.ZT02}.

Duplicate gene identifiers are preserved using
\code{\link[base:make.unique]{base::make.unique()}}. The resulting unique identifiers are stored
in the \code{matrix_gene_id} annotation column and correspond exactly to
the expression matrix row names.
}
\examples{
\dontrun{
baboon_expression <- download_gse98965()

liver <- prepare_circadian_tissue(
  expression_data = baboon_expression,
  tissue_code = "LIV",
  min_expression = 1,
  min_samples = 3
)

dim(liver$expr)
head(liver$metadata)
head(liver$gene_annotation)
}

}
