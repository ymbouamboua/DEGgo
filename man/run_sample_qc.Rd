% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{run_sample_qc}
\alias{run_sample_qc}
\title{Run sample-level RNA-seq quality control}
\usage{
run_sample_qc(
  counts,
  metadata,
  output_dir,
  annotation_cols = c("condition", "treatment", "sex", "tissue"),
  dpi = 300,
  style = "bw",
  txtsize = 12
)
}
\arguments{
\item{counts}{Numeric count matrix with genes in rows and samples in columns.}

\item{metadata}{Sample metadata data frame. Row names must match sample names
in \code{colnames(counts)}.}

\item{output_dir}{Output directory where QC files will be saved.}

\item{annotation_cols}{Character vector of metadata columns used as heatmap
annotations.}

\item{dpi}{Resolution used for the hierarchical clustering PNG.}
}
\value{
Invisibly returns a list containing the sample correlation matrix,
output directory, and paths to generated files.
}
\description{
Generate sample-level quality control outputs from a raw or filtered count
matrix, including a Spearman sample correlation heatmap, hierarchical
clustering dendrogram, and sample correlation matrix.
}
\details{
This function is designed to be used inside the DEGgo workflow after
count filtering and metadata matching. It saves all QC files directly into
the provided output directory.

The function uses \code{log2(count + 1)} transformed counts to compute Spearman
sample correlations and hierarchical clustering based on Euclidean distance.
}
\examples{
\dontrun{
qc <- run_sample_qc(
  counts = counts,
  metadata = metadata,
  output_dir = "pairwise_QC"
)
}

}
