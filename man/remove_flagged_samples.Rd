% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{remove_flagged_samples}
\alias{remove_flagged_samples}
\title{Remove flagged samples from counts and metadata}
\usage{
remove_flagged_samples(
  counts,
  metadata,
  qc_table,
  sample_col = "sample_original",
  remove_col = "recommend_remove",
  gene_cols = c("gene_id", "gene_name"),
  verbose = TRUE
)
}
\arguments{
\item{counts}{Count matrix or data frame. Rows are genes and columns
are samples. Gene annotation columns are preserved.}

\item{metadata}{Sample metadata.}

\item{qc_table}{QC table containing sample identifiers and removal flags.}

\item{sample_col}{Column containing sample names. Default is
\code{"sample_original"}.}

\item{remove_col}{Logical column indicating samples to remove.
Default is \code{"recommend_remove"}.}

\item{gene_cols}{Character vector of annotation columns to preserve.
Default is \code{c("gene_id", "gene_name")}.}

\item{verbose}{Logical; print summary messages. Default is \code{TRUE}.}
}
\value{
A list with:
\describe{
\item{counts}{Filtered counts table.}
\item{metadata}{Filtered metadata table.}
\item{removed_samples}{Vector of removed sample names.}
}
}
\description{
Removes samples flagged for exclusion in a QC table and returns
synchronized counts and metadata objects.
}
\examples{
cleaned <- remove_flagged_samples(
  counts = counts,
  metadata = metadata,
  qc_table = qc$qc
)

counts <- cleaned$counts
metadata <- cleaned$metadata

}
