% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rhythmicity.R
\name{run_deggo_rhythmicity}
\alias{run_deggo_rhythmicity}
\title{Run DEGgo rhythmicity (circadian) analysis}
\usage{
run_deggo_rhythmicity(
  expr,
  metadata,
  sample_col = "sample",
  time_col = "time",
  group_col = NULL,
  assay = c("vst", "normalized", "log2_normalized", "raw"),
  methods = c("meta2d", "cosinor"),
  period_range = c(20, 28),
  cycle_length = 24,
  cycMethod = c("ARS", "JTK", "LS"),
  padj_cutoff = 0.05,
  cosinor_engine = c("auto", "package", "manual"),
  gene_annotation = NULL,
  gene_id_col = "gene_id",
  gene_symbol_col = NULL,
  show_gene_id = FALSE,
  output_dir = "DEGgo_rhythmicity_out",
  project_name = NULL,
  generate_plots = TRUE,
  n_top_plots = 20,
  seed = 4173,
  verbose = TRUE
)
}
\arguments{
\item{expr}{A genes-by-samples numeric matrix/data frame of expression
values, or a DESeq2 \code{DESeqDataSet} object.}

\item{metadata}{Sample metadata data frame. Must contain a sample
identifier column defined by \code{sample_col} and a numeric time column
defined by \code{time_col}.}

\item{sample_col}{Column in \code{metadata} containing sample identifiers that
match the column names of \code{expr}.}

\item{time_col}{Column in \code{metadata} containing numeric time values, such
as hours or Zeitgeber Time.}

\item{group_col}{Optional column in \code{metadata} defining exactly two groups
for differential-rhythmicity testing.}

\item{assay}{Assay to extract when \code{expr} is a DESeq2 \code{DESeqDataSet}. One
of \code{"vst"}, \code{"normalized"}, \code{"log2_normalized"}, or \code{"raw"}.}

\item{methods}{Rhythmicity-detection methods to run. One or both of
\code{"meta2d"} and \code{"cosinor"}.}

\item{period_range}{Numeric vector of length two defining MetaCycle's period
search range in the same units as \code{time_col}.}

\item{cycle_length}{Assumed period for the single-component cosinor model.}

\item{cycMethod}{Character vector of MetaCycle methods to combine. Allowed
values include \code{"ARS"}, \code{"JTK"}, and \code{"LS"}.}

\item{padj_cutoff}{Adjusted p-value cutoff used to classify rhythmic genes.}

\item{cosinor_engine}{Cosinor fitting engine. \code{"auto"} uses the
\code{cosinor} and \code{cosinor2} packages when installed and otherwise falls back
to the manual \code{lm()} implementation. \code{"package"} requires the optional
packages. \code{"manual"} always uses the internal \code{lm()} implementation.}

\item{gene_annotation}{Optional gene-annotation data frame used to map
expression-matrix row identifiers to readable gene symbols.}

\item{gene_id_col}{Gene-identifier column in \code{gene_annotation}.}

\item{gene_symbol_col}{Optional gene-symbol column in \code{gene_annotation}.
When \code{NULL}, common symbol-column names are detected automatically.}

\item{show_gene_id}{Logical. Include the original gene identifier in
individual rhythmicity plot titles.}

\item{output_dir}{Output directory.}

\item{project_name}{Optional project name.}

\item{generate_plots}{Logical. Generate rhythmicity diagnostic plots.}

\item{n_top_plots}{Number of top rhythmic genes to plot individually.}

\item{seed}{Random seed.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
A \code{deggo_rhythm_results} object containing the combined summary,
method-specific result tables, matched metadata, expression matrix,
annotation settings, parameters, output paths, and generated plots.
}
\description{
Standalone rhythmicity detection workflow using MetaCycle (\code{meta2d}) and
single-component cosinor regression, with an optional cosinor-based test
for differential rhythmicity between two groups.
}
\details{
This function is independent from \code{\link[=run_deggo]{run_deggo()}} and operates on
time-course expression data supplied as either a numeric expression matrix
or a DESeq2 \code{DESeqDataSet}.
}
